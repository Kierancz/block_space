<%= form_for([@story, @story.blocks.build]) do |f| %>
  <div class="col-md-6 col-md-offset-3">
  	<h2>Add a block:</h2>
  	<div class="field">
    	<%= f.text_area :content, placeholder: "Enter HTML content..." %>
  	</div>
  	<%= f.submit "Create", class: "btn btn-large btn-primary" %>
  </div>
<% end %>